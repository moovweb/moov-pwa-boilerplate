{
  "name": "moov-pwa",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom",
    "eject": "react-app-rewired eject",
    "server": "node server/server.js",
    "build:ssr": "NODE_ENV=production webpack --config ./webpack/webpack.config.dev-ssr.js"
  },
  "devDependencies": {
  },
  "license": "ISC"
}
