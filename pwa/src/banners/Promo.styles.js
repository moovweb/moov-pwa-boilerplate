import theme from '../theme'

export default {
  promo: {
    backgroundColor: theme.palette.background.shade,
    padding: '5px 0',

    '& img': {
      width: '100vw',
      display: 'block'
    }
  }
}